interface:
  display_name: "Doc to Word Converter"
  short_description: "Batch convert documents into clean Word templates"
  default_prompt: "Use $doc-to-word-converter to process the files in my TBD folder and convert them into editable Word templates."
policy:
  allow_implicit_invocation: true
